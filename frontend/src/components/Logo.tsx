import { GiMaterialsScience } from "react-icons/gi";

const Logo = ({ className }: { className?: string }) => {
  return <GiMaterialsScience className={`text-6xl ${className}`} />;
};

export default Logo;
